# Database (Postgres)
DATABASE_URL="postgresql://user:password@localhost:5432/agora?schema=public"

# Auth.js
AUTH_SECRET="generate-a-long-random-string-here"   # openssl rand -base64 32
AUTH_URL="http://localhost:3000"

# AI Moderation (optional in dev — falls open if missing)
OPENAI_API_KEY="sk-..."
